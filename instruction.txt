// //TODO:
// Functions that I need:
// - Function that returns the complete string with the new line (get_next_line)
// - Function that reads the buffersize and returns what was read (read_buffer)
// - Function that checks if there was something left after the previous call of read_buffer and joins two strings to the static line_left (line_check)

// 1. get_next_line:
// - Checks if buffer is read, fd exists and everything is nice, otherwise returns NULL
// - Checks line_check
// while (line check == 0)
// 	read buffer();
// 	line_check;
// if (line_check > 0)
// 	return (return_line);

// 2. read_buffer:
// - reads BUFFER_SIZE characters. if everything is read correctly, returns read line.
// 3. line_check:
// - check if static var line_left is empty
// if (line_check is empty)
// 	checks the buffer for '\n'
// 	if (strchr != NULL)
// 		return_line = line till '\n';
// 		static char line_left = remainer of that(copy the remained buffer)
// 		return (1);
// 	else 
// 	{
// 		static char line_left = buffer;
// 		return (0);
// 	} 